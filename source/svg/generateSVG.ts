import { Request, Response, NextFunction } from 'express';
import axios, { AxiosResponse } from 'axios';


const generatedSVG = async (recentlyPlayed: any, steamRecentlyPlayed: any) => {
    let result: any

    return result
}

export default {generatedSVG}